# SYNCRIG 환경변수 템플릿
# 이 파일을 .env로 복사하여 실제 값을 입력하세요.

# ── JWT 보안 (필수 — Vercel Environment Variables에 설정) ──
JWT_SECRET=your_random_secret_key_here_min_32_chars
JWT_REFRESH_SECRET=your_refresh_secret_key_here

# ── 데이터베이스 (Vercel Postgres 연결 시 자동 주입) ──
# DATABASE_URL=postgresql://user:password@host:5432/syncrig

# ── 환경 구분 및 리다이렉트 (Vercel 배포 시 필수) ──
# NODE_ENV=production
# CLIENT_URL=https://your-domain.vercel.app
# SERVER_URL=https://your-domain.vercel.app

# ── Steam Web API (실제 게임 데이터 및 로그인 연동 시 필요) ──
# STEAM_API_KEY=your_steam_api_key

# ── Riot Sign On (실제 계정 연동 시 필요) ──
# RIOT_CLIENT_ID=your_riot_client_id
# RIOT_CLIENT_SECRET=your_riot_client_secret
